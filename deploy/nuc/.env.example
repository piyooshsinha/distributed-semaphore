# Copy to .env (mode 600). Never commit .env.
DISTSEM_DB_PASSWORD=
# Optional: when set, every /v1 request must send X-API-Key.
DISTSEM_API_KEY=
# Image tag to run (defaults to latest).
DISTSEM_TAG=latest
